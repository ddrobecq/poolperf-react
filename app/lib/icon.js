//import styled from 'styled-components'
import { GiPoolTriangle } from "react-icons/gi";

/*const PoolTriangleIcon  = styled(GiPoolTriangle)`
  transform: scale(2);
  margin: 5%;
`;
  */

export default function PoolIcon(props) {
    return (
        <GiPoolTriangle  fontSize={25} />
    );
}
